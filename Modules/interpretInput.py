# this should take in an input name, ie. dew, mnt, qcf, etc, and output the corresponding input from the json file
# also takes in the character name because some characters have inputs with the same name but different inputs, like how multiple characters have a backturn stance with different ways to access it
def interpretInput(characterName, inputName, jsonFile):
    # open json file

    # open the character name object in json

    # open the corresponding input

    # output the input string
    output = ""

    return output